pipeline {
    agent {
        dockerContainer {
            image 'python-test-image'  
        }
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Test') {
            steps {
                script {
                    try {
                        sh 'python3 -m unittest discover'  // Utilise python3 pour exécuter les tests
                    } catch (Exception e) {
                        echo "Tests failed: ${e}"
                        currentBuild.result = 'FAILURE'
                    }
                }
            }
        }
    }

    post {
        success {
            echo 'Tests passed. Merging dev into master.'
            sh '''
                git config --global user.email "your-email@example.com"
                git config --global user.name "Your Name"
                git checkout master
                git merge --ff-only dev
                git push origin master
            '''
        }
        failure {
            echo 'Tests failed. Moving commit to failures branch.'
            sh '''
                git config --global user.email "your-email@example.com"
                git config --global user.name "Your Name"
                git checkout dev
                git reset HEAD~1 --hard
                git checkout -b failures/${BUILD_NUMBER}
                git push origin failures/${BUILD_NUMBER}
            '''
        }
    }
}
